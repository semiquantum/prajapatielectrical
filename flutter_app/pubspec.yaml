name: prajapati_electrical_mobile
description: Enterprise Mobile Application for Prajapati Electrical (Owner & Employee portals).
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  supabase_flutter: ^2.5.0
  flutter_secure_storage: ^9.0.0
  fl_chart: ^0.66.0
  image_picker: ^1.0.7
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
